[package]
name = "zooma"
version = "1.2.0"
edition = "2024"

[profile.dev]
debug = "line-tables-only"

[dependencies]
raylib = { version = "6.0", features = ["wayland"] }
serde = { version = "1.0.229", features = ["derive"] }
toml = "1.1.4"
